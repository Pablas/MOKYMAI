let miestas = 'Klaipeda';

// ar zmogus nera is vilniaus arba kauno arba klaipedos

if (!(miestas == 'Vilnius' || miestas == 'Kaunas' || miestas == 'Klaipeda')){
    console.log('Zmogus nerastas')
} else if (miestas == 'Vilnius' || miestas == 'Kaunas' || miestas == 'Klaipeda') {
    console.log ('Zmogus is vieno is didziausiu miestu'
    )
}